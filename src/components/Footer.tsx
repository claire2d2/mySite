import React from "react";
import Dim from "../assets/CssStyles";

const Footer = () => {
  return (
    <footer style={{ height: `${Dim.FooterHeight}` }}>This is a footer!</footer>
  );
};

export default Footer;
