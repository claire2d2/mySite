import React from "react";
import Dim from "../assets/CssStyles";

const NavBar = () => {
  return (
    <nav
      className={`NavBar bg-slate-900 text-white p-4 pl-10 pr-12 w-screen`}
      style={{ height: `${Dim.NavBarHeight}` }}
    >
      Website under construction!
    </nav>
  );
};

export default NavBar;
